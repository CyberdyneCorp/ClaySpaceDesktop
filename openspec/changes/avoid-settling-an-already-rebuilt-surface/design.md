## Invariant

A store built by one meshing request has one consistent triangle ownership assignment. Merging partial requests can leave boundary copies under different keys. Full-gradient document meshes evaluate attributes by vertex position, independently of request ownership; their exact duplicates can be compacted without evaluating the field again. Fast preview normals depend on the requested neighborhood and require the existing rebuild path. Track whether every retained triangle uses document-gradient shading, and permit release compaction only on a synchronized full-resolution document surface without a cage preview.

Track a needs-settle bit. On a successful remesh replacing all stored triangles, or the full rebuild path, clear it. On a partial remesh retaining old triangles under unreplaced keys, set it. Empty bookkeeping entries do not count as retained geometry. This also recognizes undo requests that replace the entire surface despite using the dirty-key path. An empty full rebuild also clears it. Attribute-only mask/cage changes and diagnostic layout compaction preserve it. The release-specific compaction clears debt only after successful upload; it preserves every distinct complete vertex value. Explicit rebuild and settle retain their full-remesh semantics. The initial sync uses a partial-request argument but starts with an empty store; it must count as one request, not manufacture initial settlement debt.

Keep the existing EndStroke debt and live-gesture guard. When flushing allowed debt, clear the host flag and rebuild only if the geometry still needs settlement. If a preview-to-document epoch change already rebuilt it, that rebuild satisfied the debt. Do not bypass explicit rebuild/settle calls.

## Verification

First require a measured mask release to perform zero uploads after its measured attribute update; the old unconditional settlement must fail. Verify initial single-request sync, partial updates requiring settlement, explicit full/empty rebuilds clearing it, and compaction retaining it. Retain rendered comparisons and live-gesture guard tests. Measure both mask-only and epoch-changing releases, reporting real remaining work. No universal 16 ms claim.

## Release compaction follow-up

Read-only probes on the pinned engine show exact equality between incremental and rebuilt triangle sets for eight ordinary brushes (24 dabs each), and the existing long mixed session (588,184 distinct triangles, 11,281 duplicate copies). Compare all ten float bit patterns per vertex, not quantized positions. Keep permanent coverage for repeated compaction followed by further edits. A separate release method uses compaction only when all retained triangles have full document-gradient shading, the surface epoch matches, full detail is drawn and no cage preview is active. An open live gesture remains deferred. Ineligible surfaces use full settlement. Record a distinct compaction route with zero engine mesh/read time so measurement can verify the omission.

Release compaction also reclaims storage previously discarded by a full rebuild: remove empty brick entries and unreferenced vertices, remap surviving indices without changing vertex bits or triangle order, and release spare vector capacity. Ordinary duplicate-pruning diagnostics retain their existing vertex-array contract. Prepare release geometry once, then lay it out without a second duplicate pass.
