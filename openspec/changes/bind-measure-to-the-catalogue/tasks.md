## 1. Bind the builder to the table

- [x] 1.1 `accept_declared` refuses an undeclared group and an action with no row.
- [x] 1.2 `measure_refuses_an_undeclared_group` and `a_route_without_a_row_is_refused_whoever_builds_it`.

## 2. Background work is outstanding

- [x] 2.1 `Measured.outstanding`, filled from `outstanding_work`, and the measure note.
- [x] 2.2 `work_started_by_measure_is_outstanding`.

## 3. Say what is withheld and what each action does

- [x] 3.1 `not_offered` lists all eleven withheld commands; `not_offered_is_complete` reads `home_of`.
- [x] 3.2 Correct the `layer.set_combine`, `lattice.drag`, `transform.drag`, `brush.set_flow`, `brush.set_smoothing` and repair summaries; `snap` replaces `invert`.
- [x] 3.3 `each_corrected_summary_matches_the_action` and `dynamics_actions_are_offered`.
- [x] 3.4 README states that `measure` is bound by the catalogue and lists the work it left running.
