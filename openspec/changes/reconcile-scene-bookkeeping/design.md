# Design

Keep the engine document authoritative. After a consolidation, compare each placed object's layer and node with the document and update the object table under the same history stamp. Clear selection when its object leaves. Validate selections against the current object list. Check all layer names at the document boundary using a 128-character limit. Mark the visible field's affected bounds across visibility transitions and drain the cache. Explain refusals from the actual visibility and protection state.
