# Tasks

- [x] Reproduce the ungated Snake Hook, the moving root, the overshooting tip and the seam on the starting form.
- [x] Gate the tendril by the mask; drop the link blend for a surface fillet; trim the tip; set the curve tolerance.
- [x] Reproduce the folded topological drag and the masked band it moved.
- [x] Step the topological drag, widen its band, gate it by the mask, and hold it whole on a field.
- [x] Add regressions in `clayspace-engine` (`snake_hook`, `topological_move`) and `clayspace-model`.
- [x] Update the specification and documentation.
