# Tasks

## 1. Withdraw the tool
- [x] 1.1 Remove `ToolKind::MoverTopologico` and its verb-table row
- [x] 1.2 Remove its glyph, key, label and the three languages' name and hint
- [x] 1.3 Remove the SDF stroke routing and `topological_move_stroke`
- [x] 1.4 Rehome the table tests that used it as their example

## 2. Pin the outcome
- [x] 2.1 A visual test over every tool the shelf offers on a field, measured
      against an untouched sphere rendered in the same run
- [x] 2.2 Confirm it fails with the tool restored and passes without it
